<%= form_for list do |f| %>
  <p>
    <%= f.text_field :name %>
    <%= f.select :list_id, "<option value=\"\"></option>" + options_from_collection_for_select(lists, 'id', 'name') %>
    <%= f.submit 'Save' %>
  </p>
<% end %>